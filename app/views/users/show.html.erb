<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title></title>
	</head>
	<body>
		<p>
			Hello <%= @user.username %>! </br>
			You have <%= @user.unredeemed_points %>  Points! <br />
			You've spent <%= @user.points.count - @user.unredeemed_points %> Points on
			some sweet rewards!
		</p>
		<p>
			Your rewards: <br/> <% @user.user_rewards.each do |user_reward| %>
			<h5><%= user_reward.reward.title %> <br /> <div>
				<img src=<%= user_reward.reward.url %>></h5>
			</div> 
			<% end %>
		</p>
		<p>
			<%= link_to "Rewards Store", rewards_path %> <br />
			<%= link_to "Dashboard", root_path %>
		</p>
	</body>
</html>
<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title></title>
	</head>
	<body>
		<%= link_to "Back to Dashboard", root_path %>
	<% @users.each do |user| %>
  <li >
    <%= link_to user.username, user_path(user) %> <br />
		Points: <%= user.points.count %>
		<%= link_to "Add Point", user_points_path(user), method: 'post' %>
  </li>
  <% end %>
	</body>
</html>
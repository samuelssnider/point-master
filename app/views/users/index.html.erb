<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title></title>
	</head>
	<body>
	<% @users.each do |user| %>
  <li >
    <%= link_to user.username, user_path(user) %> <br />
		Points: <%= user.points.count %>
  </li>
  <% end %>
	</body>
</html>